name: Deploy
on:
  push:
    branches:
      - grow_heads

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout 🛎
        uses: actions/checkout@v2
        with:
          persist-credentials: false

      - name: Install SSH key
        run: |
          mkdir -p ~/.ssh/
          echo -e 'Host *\n\tStrictHostKeyChecking no\n\n' > ~/.ssh/config
          echo "${{ secrets.SSH_GH_KEY }}" > ~/.ssh/id_rsa
          chmod 700 ~/.ssh
          chmod 400 ~/.ssh/id_rsa

      - name: Deploy 🚀
        run: |
          docker network create --driver bridge server || true
          docker-compose up -d --build --remove-orphans
        env:
          DOCKER_HOST: ssh://${{ secrets.SSH_GH_USER }}@${{ secrets.SSH_GH_HOST }}
